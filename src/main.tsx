import React from "react";
import ReactDOM from "react-dom/client";
import UnifiedRelationsApp from "./UnifiedRelationsApp";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <UnifiedRelationsApp />
  </React.StrictMode>
);
